# Copy to .env (never committed; the PreToolUse hook blocks the agent from editing .env*).
# Context7 MCP (current library docs): free key from https://context7.com/dashboard — 1,000 calls/month.
# Anonymous access shares a small global pool and gets rate-limited, so the live demo needs a key.
CONTEXT7_API_KEY=ctx7sk-...
